---
:backends:
    - yaml
:yaml:
    :datadir: '/var/lib/hieradata'
:hierarchy:
    - %{::customer_name}/%{::customer_site}/%{::customer_build}
    - %{::customer_name}/%{::customer_site}
    - %{::customer_name}
    - common